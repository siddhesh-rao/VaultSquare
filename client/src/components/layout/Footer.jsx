const Footer = () => (
  <footer className="border-t border-white/10 py-6">
    <div className="page-shell flex flex-col gap-2 text-sm text-slate-400 sm:flex-row sm:items-center sm:justify-between">
      <p>VaultSquare blends retail checkout with blockchain-backed rental agreements.</p>
      <p>Built with React, Express, MongoDB, Hardhat, and MetaMask.</p>
    </div>
  </footer>
);

export default Footer;
